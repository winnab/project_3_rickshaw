!!!
%html
  %head
    %meta{content: "initial-scale=1.0, user-scalable=no", name: "viewport"}/
    :css
      html { height: 100% }
      body { height: 100%; margin: 0; padding: 0 }
      #map-canvas { height: 100% }
    %script{src: "https://maps.googleapis.com/maps/api/js?key=AIzaSyADuhmURB0to7p_wcihExmjqVolRbUZ96k&sensor=true", type: "text/javascript"}
      :cdata
    :javascript
      function initialize() {
        var mapOptions = {
          center: new google.maps.LatLng(37.7719474, -122.4244384),
          zoom: 15
        };
        var map = new google.maps.Map(document.getElementById("map-canvas"),
            mapOptions);
      }
      google.maps.event.addDomListener(window, 'load', initialize);
  %body
    #map-canvas